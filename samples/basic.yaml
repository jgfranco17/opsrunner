---
# Project metadata
name: OpsRunner
version: 0.1.0
description: A simple CLI tool to run tasks defined in a YAML configuration file.
repo_url: www.github.com/jgfranco/opsrunner

# Test steps, run before build
test:
  category: test
  env:
    IS_TEST: "true"
  steps:
    - ls -la
    - echo "Hello World!"

# Build steps, assembles the project
build:
